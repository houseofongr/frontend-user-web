import { useState } from "react";
import { useNavigate } from "react-router-dom";
import API_CONFIG from "../../config/api";

export default function TermsPage() {
  return <div className="">이용약관동의페이지</div>;
}
